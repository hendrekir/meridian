[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r backend/requirementsv8.1.txt"]

[start]
cmd = "cd backend && uvicorn indexv8.1:app --host 0.0.0.0 --port $PORT"
