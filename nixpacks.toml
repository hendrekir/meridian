[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["python3.11 -m pip install -r backend/requirementsv8_3_6.txt"]

[phases.build]
cmds = []

[start]
cmd = "uvicorn app:app --host 0.0.0.0 --port $PORT"
