[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["python3.11 -m pip install -r backend/requirementsv8.3.2.txt"]

[start]
cmd = "uvicorn backend.indexv8.3.2:app --host 0.0.0.0 --port $PORT"
